[blog]

; The blogs name
;
blog.name = Tests

; The path to the Markdown files
;
blog.files_path = tests/posts

; Number of posts to show per page
; (Coming soon)
blog.items_per_page = 10

; The extension of the Markdown files
;
blog.file_extension = md

; How should articles be sorted
;
blog.sort_key = date